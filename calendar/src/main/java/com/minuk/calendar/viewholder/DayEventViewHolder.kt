package com.minuk.calendar.viewholder

import androidx.recyclerview.widget.RecyclerView
import com.minuk.calendar.databinding.ItemCalendarDayBinding
import com.minuk.calendar.model.Event

internal class DayEventViewHolder(
    val binding: ItemCalendarDayBinding
) : RecyclerView.ViewHolder(binding.root) {

    fun bind(event: Event) {

    }
}