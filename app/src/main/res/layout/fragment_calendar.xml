<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="com.minuk.petcalendar.calendar.CalendarViewModel" />

        <variable
            name="viewModel"
            type="CalendarViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".calendar.CalendarFragment">

        <TextView
            android:id="@+id/currentMonthTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_marginTop="20dp"
            android:background="?android:selectableItemBackground"
            android:onClick="@{() -> viewModel.setCurrentMonth()}"
            android:padding="5dp"
            android:text="@{viewModel.currentMonthText}"
            android:textColor="@android:color/black"
            android:textSize="@dimen/main_calendardate_textsize"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="2021년 1월" />

        <ImageView
            android:id="@+id/imageView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_arrow_down"
            app:layout_constraintBottom_toBottomOf="@id/currentMonthTextView"
            app:layout_constraintStart_toEndOf="@id/currentMonthTextView"
            app:layout_constraintTop_toTopOf="@id/currentMonthTextView"
            tools:ignore="ContentDescription" />

        <com.minuk.calendar.PetCalendarView
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginTop="20dp"
            app:calendarColorAccount="@color/main_accent"
            app:calendarColorNormal="@color/main_normal"
            app:calendarDate="@{viewModel.calendarDate}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/currentMonthTextView" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>